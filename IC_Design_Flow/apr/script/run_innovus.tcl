# ============================================================
# Innovus APR 模板（示意）
# ============================================================

set TOP  "DESIGN_TOP"
set ROOT [file normalize [file join [pwd] ../..]]

# set init_verilog [file join $ROOT syn netlist ${TOP}_syn.v]
# set init_top $TOP
# # init_design / floorplan / place / ccopt / route ...
# defOut [file join $ROOT apr def ${TOP}.def]
# streamOut [file join $ROOT apr gds ${TOP}.gds]

puts "INFO: Innovus template — fill in process tech & flow commands."
exit
